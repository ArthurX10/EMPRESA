1º Exibir todos os dados das categorias.
SELECT * FROM Categorias


Exibir o nome de todos os clientes ordenados pelo nome.
SELECT nome
from Clientes
ORDER by nome

Exibir o nome dos funcionários de cargo gerente (existem vários gerentes, por exemplo gerente
de vendas, gerente de marketing), que residem no Brasil e cujo salário é superior a R$ 1500.select * from [dbo].[Funcionarios]	

where Cargo like 'Gerente%' 
	  and Pais like 'Brasil' 
	  and salario > 1500

select * from [dbo].[Produtos]
where Descontinuado is not null		
	  and Preco between 10 and 100
	  and Unidade s is not null


select Empresa as Fornecedor, Contato as Telefone  from [dbo].[Fornecedores]
where Pais in ('Itália', 'Noruega', 'Alemanha','Reino Unido') 
order by Fornecedor


SELECT MAX(Salario) as maior,
	   MIN(Salario) as menor,
	   AVG(Salario) as media
FROM [dbo].[Funcionarios]


SELECT max(SALARIO) AS MEDIA FROM [dbo].[Funcionarios]
WHERE [Cargo] LIKE 'Gerente%'
	  and Pais = 'Brasil'


select max(salario) as maior from [dbo].[Funcionarios]
where Cidade in ('Tianguá', 'Viçosa', 'Ubajara', 'Ibiapina')
group by Salario
order by Salario

select avg(salario) as media from [dbo].[Funcionarios]

SELECT numped as numero_pedido, SUM(preco) AS preco_total
FROM DetalhesPed
WHERE preco = (preco * qtde) - desconto
GROUP BY numped
ORDER BY numped;

select unidades, count(codcategoria)
from Produtos
where unidades > 10
GROUP by unidades
ORDER by unidades;

SELECT codfornecedor, COUNT(*) AS quantidade_produtos
FROM Produtos
WHERE preco_unitario > 5.00 AND preco_unitario < 10.00
GROUP BY codfornecedor
ORDER BY quantidade_produtos ASC;

select SUM(salario) as Total, cargo
from Funcionarios
where cargo in('Gerente', 'Coordenador')
GROUP by cargo
ORDER by Total 

SELECT pais ,AVG(salario)
from Funcionarios
GROUP by pais

SELECT COUNT(*) as Quantidades, AVG(preco) as media 
from Produtos
WHERE descontinuado is not null
GROUP by codcategoria
ORDER by media

